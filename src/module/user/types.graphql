scalar Date

type User{
    _id: ID!
    originalId: ID!
    name: String!
    email: String!
    password: String
}

type TRAINEELIST {
    TraineeCount: String
    data: [ALLFIELD]
}

type ALLFIELD {
    name: String
    email: String
    role: String
    originalId: String
    _id: String
    createdAt: Date
}